CREATE TABLE EMPLOYEE(
    EMPNO NUMBER(2),
    ENAME VARCHAR2(21),
    JOB VARCHAR2(30),
    MGR NUMBER(2),
    SAL NUMBER(7,2),
    CONSTRAINT EPKEY PRIMARY KEY(EMPNO),
    CONSTRAINT FKEY FOREIGN KEY(MGR) REFERENCES EMPLOYEE(EMPNO)
);

ALTER TABLE EMPLOYEE ADD COMMISSION NUMBER(4);
INSERT INTO EMPLOYEE VALUES(001,'HARY','DEV',01,10000,1000);

UPDATE EMPLOYEE SET JOB='MGR' WHERE EMPNO=10;

DELETE FROM EMPLOYEE WHERE EMPNO=10;

ALTER TABLE EMPLOYEE RENAME COLUMN SAL TO SALARY;